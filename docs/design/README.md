# Design artifacts

`codex-switch-icon-concept.png` is an exploratory white ascending quota-bar
glyph with a switch arrow. It is a composited ImageGen preview (1254px RGB,
without an alpha channel), not a shippable application asset and not wired into
the app. The development plan requires a separate transparent master plus
small-size, alpha, and Computer Use visual checks before any icon ships.
