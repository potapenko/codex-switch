# Feature specs

Active specs selected by `index.md` are the source of truth for intended
behavior. Code and tests are evidence, not a silent replacement for a spec.

For a behavior change, update the relevant spec before changing source.
